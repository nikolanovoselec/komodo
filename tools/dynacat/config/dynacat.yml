# Dynacat is protected by Cloudflare Access. Do not expose this port publicly
# without an equivalent access-control layer.

server:
  port: 8080
  assets-path: /app/assets
  cache-dir: /app/assets/.cache
  allow-editing: false

pages:
  - name: Home
    columns:
      - size: full
        widgets: []
