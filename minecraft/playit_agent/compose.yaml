services:
  minecraft_playit:
    container_name: minecraft_playit_agent
    image: ghcr.io/playit-cloud/playit-agent:latest
    network_mode: bridge
    environment:
    - SECRET_KEY=${TOKEN}
    restart: unless-stopped