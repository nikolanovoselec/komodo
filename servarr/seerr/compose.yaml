services:
  seerr:
    image: ghcr.io/seerr-team/seerr:v3.1.0
    #init: true
    container_name: servarr_seerr
    #dns:
    #  - 100.64.0.2
    #  - 192.168.5.162
    #  - 192.168.5.92
    environment:
      - LOG_LEVEL=info
      - TZ=Europe/Zurich
      - PORT=5054 #optional
    #user: root
    ports:
      - 5054:5054
    volumes:
      - /docker/configs/seerr:/app/config
    healthcheck:
      test: wget --no-verbose --tries=1 --spider http://localhost:5054/api/v1/status || exit 1
      start_period: 20s
      timeout: 3s
      interval: 15s
      retries: 3
    restart: unless-stopped