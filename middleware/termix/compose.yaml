services:
  termix:
    image: ghcr.io/nikolanovoselec/termix:feature-oidc-import
    container_name: middleware_termix
    restart: unless-stopped
    ports:
      - "8079:8080"
    volumes:
      - /docker/configs/termix:/app/data
    environment:
      PORT: "8080"