services:
  backrest:
    image: garethgeorge/backrest:v1.12.1
    container_name: dns_ntp_backrest
    hostname: backrest-pihole-master.local.graymatter.ch
    volumes:
      - /docker/configs/backrest/data:/data
      - /docker/configs/backrest/config:/config
      - /docker/configs/backrest/cache:/cache
      - /docker/configs/backrest/tmp:/tmp
      - /docker/configs/:/userdata #Local mount to be backed up
      - /mnt/unas/Backup/Restic:/repos #NFS mount of the UNAS restic repository
    environment:
      - BACKREST_DATA=/data
      - BACKREST_CONFIG=/config/config.json
      - XDG_CACHE_HOME=/cache
      - TMPDIR=/tmp
      - TZ=Europe/Zurich
    ports:
      - "9898:9898"
    restart: unless-stopped
    network_mode: bridge
